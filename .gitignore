# 연동 키가 든 설정 — 공개 저장소에 올리지 말 것 (SETUP-광고관리자.md 참고)
config.js

# Supabase CLI 로컬 임시 파일
supabase/.temp/
.branches/
.claude/
.insight-tmp/
scripts/.bin/

# 셀메이트 연결 스크립트 — 공개 저장소에 올리지 않음 (상담원에게 파일로 전달)
cs-sellmate-bridge.user.js
