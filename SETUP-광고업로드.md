# 광고 업로드 (Meta) 셋업

Ads Uploader를 대신하는 메뉴. 모델 광고 1개를 고르면 그 광고세트 설정(타겟·최적화·픽셀)·페이지·강화옵션을 그대로 복사해
**파일 1개 = 광고세트 1개(예산만 입력) + 광고 1개**로 만든다. 문구는 직접 기입, 일시중지/바로 활성 선택.

서버: `supabase/functions/meta-upload` (쓰기 토큰은 서버에만, DASH_KEY + PIN). 예산 변경 기능과 같은 `META_WRITE_TOKEN`·`WRITE_PIN`을 쓴다.

## 1. 시스템 사용자에게 페이지 권한 주기 (최초 1회 — 이게 없으면 크리에이티브 생성이 거부됨)

[연결 진단] 실측 오류: `이 프로필에 액세스하는 데 필요한 권한이 없습니다` / `광고주 권한이 있어야 합니다`
→ 쓰기 토큰의 시스템 사용자(**mk-dashboard**)에 페이지·인스타그램 자산이 배정돼 있지 않아서다.

1. https://business.facebook.com/settings → **사용자 › 시스템 사용자** → `mk-dashboard` 선택
2. **자산 추가** → **페이지** → 다나로브 페이지 체크 → 권한 **광고 만들기**(또는 전체 관리) → 저장
3. 같은 화면에서 **Instagram 계정** → `danarobe_` 체크 → 광고 만들기 권한 → 저장
4. **토큰 생성** → 앱 선택 → 권한 체크: `ads_management`, `business_management`, `pages_read_engagement`, `pages_manage_ads` → 생성 → **[복사]**
5. 복사 직후 바탕화면(또는 이 폴더)의 **`쓰기토큰-갱신.command`** 더블클릭 → secret 등록 + 함수 재배포 (토큰은 클립보드에서만 읽고 바로 비움)
6. 대시보드 → **광고 업로드** → 모델 광고 선택 → **[연결 진단]** → 세트·크리에이티브·광고 세 줄 모두 `ok`면 끝

## 1-2. 토큰 발급 앱을 라이브(공개) 모드로 (최초 1회)

[연결 진단] 실측 오류: `광고 크리에이티브 게시물이 개발 모드인 앱에서 만들어졌습니다. 공개 모드여야 합니다.`
→ 토큰을 발급한 앱 **mk-dashboard (앱 ID 2252150572238444)** 가 개발 모드라서 크리에이티브 생성이 거부된다. 세트·광고 검증은 통과.

1. https://developers.facebook.com/apps/2252150572238444/settings/basic/ 접속 (페이지 권한을 준 계정으로 로그인)
2. **개인정보처리방침 URL** 칸이 비어 있으면 채운다 — 쇼핑몰 하단 '개인정보처리방침' 링크 주소(다나로브 사이트의 것)를 그대로 붙여넣으면 됨 → **변경 내용 저장**
3. 화면 맨 위 **앱 모드: 개발** 토글을 **라이브**로 전환 → 확인
4. 대시보드 → 광고 업로드 → [연결 진단] → 크리에이티브 줄까지 `ok` 확인. 토큰 재발급은 필요 없음.

## 2. 사용법

1. **① 모델 광고**: 캠페인 › 광고세트 › 광고 순으로 클릭 (보통 '테스트' 캠페인의 직전 테스트 광고). 요약에 최적화·타겟·예산·문구가 뜬다.
2. **② 파일**: 끌어다 놓기. 파일명(확장자 제외)이 세트명·광고명 — 표에서 수정 가능. 영상은 Meta 처리 완료까지 자동 대기.
3. **③ 설정**: 세트당 일예산(모델 세트 예산이 기본, 1,000~300,000원) · 세트 일시중지(광고는 활성이라 세트만 켜면 게재)/바로 활성 · **광고 문구 일괄 기입**(모든 파일) 또는 행별 **기입** · PIN(예산 변경 PIN과 동일)
4. **광고 생성** → 확인창 → 파일별로 업로드→세트→크리에이티브→광고 순차 생성, 로그 표시. 실패한 파일만 다시 눌러 재시도 가능.

## 3. 알아둘 것

- 인스타그램 노출 위치는 모델 세트 그대로 전부 복사된다(피드·스토리·릴스·탐색 홈·프로필 피드·검색 결과). 단 Meta가 이미 폐지해서 광고관리자에서도 선택할 수 없는 옛 항목 '탐색 탭(explore)' 값이 오래된 세트 데이터에 남아 있으면 그 값만 빼고 보낸다 — 안 빼면 Meta가 생성 자체를 거부(실측). 노출 범위가 줄어드는 게 아니다.
- 모델 크리에이티브 강화옵션의 `standard_enhancements` 묶음 키도 v23에서 지원 중단이라 자동으로 빼고, 개별 기능(inline_comment 등)만 그대로 복사한다.
- CBO(캠페인 예산) 캠페인이면 세트 예산 입력이 필요 없고 자동으로 비활성.
- 미디어는 3개씩 동시에 전송하고(파일끼리 병렬), 광고 생성은 순서대로. 영상은 8MB 조각으로 서버를 거쳐 Meta 재개 가능 업로드 — 서버가 조각을 거부하면 자동으로 절반씩 줄여 재시도(최소 1MB). 로그에 전송 소요 시간이 찍힌다.
- 강화옵션(`degrees_of_freedom_spec`)·URL 태그·페이지·인스타 계정은 모델 크리에이티브 값 그대로. 다이나믹/플렉시블 소재를 모델로 고르면 문구 복사가 불완전할 수 있어 경고 표시.
- 실패 시 서버 오류 메시지가 Meta 원문 그대로 로그에 뜬다.
