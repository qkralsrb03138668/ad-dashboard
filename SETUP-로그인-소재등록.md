# 로그인 계정 · 소재 등록 셋업 (2026-09-07)

## 1. 최초 관리자 계정 만들기 (대표님, 1회)
1. **로컬 폴더의 index.html**(config.js가 있는 것)을 연다 → 계정이 하나도 없으면 "최초 관리자 계정을 만드세요" 화면이 뜬다.
2. 이름·이메일·비밀번호(8자+) 입력 → **관리자 계정 만들기** → 자동 로그인.
   - "나중에 만들기"를 누르면 예전처럼 접근키(config.js)로 계속 쓸 수 있다. 다른 PC에서 쓰려면 반드시 계정을 만들어야 한다.
3. 배포판 https://qkralsrb03138668.github.io/ad-dashboard/ 에서도 이 계정으로 로그인하면 실데이터가 나온다 (config.js 불필요).

## 2. 마케터 계정 만들기
데이터 관리 → **사용자 관리** → 이메일·이름·임시 비밀번호·역할(마케터) → 계정 만들기 → 본인에게 이메일과 임시 비밀번호 전달.
로그인 후 상단 이름 옆 **[비밀번호 변경]** 으로 바꾸게 안내. 역할 변경·삭제도 같은 표에서.

| 역할 | 보이는 메뉴 | 할 수 있는 것 |
|---|---|---|
| 관리자 | 전부 | 광고 생성·예산 변경(PIN)·판매 성과·사용자 관리 |
| 마케터 | 대시보드·소재 비교·광고소재 대시보드·소재 목록·테스트 소재 | 소재 등록(등록 PIN)·체크보드 |

## 3. 등록용 PIN (1회)
`등록PIN-설정.command` 더블클릭 → 숫자 PIN 입력 → 저장. 마케터에게 이 PIN만 전달 (광고 생성 PIN은 별개).
등록 PIN이 없으면 미디어 업로드에 광고 생성 PIN이 필요하다.

## 4. 소재 등록 흐름 (마케터)
광고소재 대시보드 → **소재 등록** 박스에 파일 드롭.
- 파일명 규칙: `상품명_나머지.확장자` — 첫 밑줄 앞이 상품명. 예 `뉴 내티 원피스_인스타 260907.mp4`
- **핸드폰처럼 파일명을 못 바꿀 때**: 드롭 영역 위 **상품 먼저 고르기**에서 상품을 검색해 고른 뒤 파일을 올리면 파일명과 상관없이 그 상품으로 등록된다. 등록 파일명은 규칙대로 자동 생성: **`상품명_R1|P1_마진_YYMMDD_test.확장자`** — 상품명은 괄호 묶음 뺀 핵심명(괄호 안에 `여름ver`·`기모ver.`·`봄.가을VER.` 같은 버전 표기가 있으면 `여름ver 레이스 클레르 블라우스`처럼 앞에 붙임), R=영상/P=이미지+순번(같은 상품에 이미 등록된 것에 이어서), 마진=(판매가−공급가) 천 원 단위 내림(28,500→28), 날짜=오늘. 소구점(착용컷·인스타·다나대표 등)을 상품 고르는 칸 옆에 적으면 순번 뒤에 들어간다: `레이스 클레르 블라우스_R3_착용컷_28_260911_test.mp4`. 행마다 따로 고칠 수도 있다. 광고세트·광고명이 된다. [해제]하면 다시 파일명 자동 인식.
- 카페24 상품명의 괄호 묶음 `( )` `[ ]`을 뗀 이름과 대조한다. `(made,high quality) 안스 후드 집업 (3 colors)` → 안스 후드 집업.
- **정확히 1개**면 자동 확정 + 상품 URL(`…/product/detail.html?product_no=번호`) 자동 기입.
- **같은 이름 상품이 여러 개**(뉴 내티 원피스 여름ver / 봄가을ver)면 노란 행 + 드롭다운으로 고르게 한다. 한 번 고르면 다음부터 그 선택이 기본값(product_alias).
- **못 찾으면** 비슷한 후보 드롭다운 + 상품명 검색창.
- 문구는 행별 [기입], 체크 후 [선택 문구 기입], [전체 문구 기입]. 여러 파일에 넣을 때 URL은 파일별 상품 URL이 유지된다. 등록 후에도 '등록된 소재' 표에서 수정 가능.
- 등록 PIN 입력 → **등록** → 파일은 Meta 광고 계정 보관함으로(3개씩 동시 전송), 기록은 서버(creatives)로.
- 체크보드: 이미지 → **스토리** 칸, 영상 → **릴스** 칸이 자동으로 '제작완료 N개'가 되고, 광고가 만들어지면 '진행중'. 자동 칸은 클릭으로 못 바꾼다. 다른 유형(썸네일릴스 등)은 예전처럼 수동.

## 4-2. 상품별 광고 문구 자동 채움 · AI 생성 (2026-09-10)
- **Claude API 키 등록(1회)**: console.anthropic.com → API Keys → Create Key → [복사] 직후 `AI키-등록.command` 더블클릭. 쓴 만큼 과금(종량제), 문구 1개당 소액.
- 소재 등록에서 상품이 정해지면 **그 상품에 저장된 문구가 있으면 바로 채워진다**(버튼에 '저장 문구' 표시). 없으면 행의 **[AI]** 버튼이나 체크 후 **[선택 AI 문구 생성]**으로 생성 → 생성 즉시 그 상품에 고정. 같은 상품 여러 파일은 1회만 생성.
- AI 지시문은 클로드 앱 세션 "다나로브 메타/인스타 광고 문구"의 프롬프트 원문 + 긴글 기본 규칙 + 대표가 준 실제 글 예시 19개를 그대로 옮긴 것(`supabase/functions/cafe24-perf/ad-copy-prompt.ts`). 모델 claude-opus-5, 상품 페이지(web_fetch)와 카페24 상품 정보를 근거로 씀.
- 행별 [기입]으로 직접 쓰거나 고친 문구, '등록된 소재' 표에서 고친 문구도 그 상품에 고정된다(다음 등록부터 그 문구). 여러 파일 일괄 기입은 상품이 섞이므로 고정하지 않음.
- 광고 업로드 탭의 등록 소재에 문구가 비어 있으면 상품 고정 문구를 자동으로 채운다.
- 키가 없으면 [AI] 버튼은 "ANTHROPIC_API_KEY 미설정" 오류만 내고 등록은 그대로 진행된다.

## 4-3. API 결제 없이 문구 생성 — `문구생성.command` (대표님 맥, 구독 사용)
서버 [AI] 버튼은 API 키(별도 과금)가 있어야 한다. 키 없이 쓰려면:
1. 마케터가 소재를 등록한다 (문구는 비워도 됨).
2. 대표님이 프로젝트 폴더의 **`문구생성.command`** 더블클릭 → 광고 업로드 대기 소재 중 **문구·저장본이 없는 상품만** 골라, 이 맥에 로그인된 Claude Code(구독)로 긴글을 생성 → 상품에 고정(product_copy) + 그 소재들 문구 채움. 저장본이 이미 있는 상품은 생성 없이 소재에 채우기만 한다.
3. 광고 업로드 탭을 새로고침하면 문구가 들어와 있다. 다듬으려면 행의 [기입] — 저장하면 **같은 상품의 대기 소재 전부와 상품 고정 문구가 같이 바뀐다** ('등록된 소재' 표에서 고쳐도 동일).
4. **특정 소재만 새로 만들기**: 광고 업로드 탭에서 소재를 체크 → **[선택 소재 문구 다시 생성]** → '재생성 대기' 표시 → 바탕화면 `문구생성.command` 더블클릭(엔터) → 그 상품만 새 문구로 만들어 상품 저장본을 덮어쓰고, 그 상품의 대기 소재 전부에 넣는다.
- 시험 삼아: `node scripts/gen-copy.mjs --dry 2450` → 저장 없이 화면에만 출력.
- 전제: 로컬 config.js(DASH_KEY) + 터미널에서 `claude`가 로그인돼 있을 것. 지시문은 서버와 같은 `ad-copy-prompt.ts`.

## 5. 광고 생성 (관리자)
광고 업로드 → ① 모델 광고 선택 → ② **등록된 소재(대기)가 자동으로 들어와 있음**(전송 불필요, 파일 직접 추가도 가능) → ③ 예산·상태·PIN → 광고 생성.
생성되면 소재 기록이 '광고 생성됨'으로 바뀌고 체크보드는 '진행중'.

## 알아둘 것
- 서버 함수 인증이 접근키 단일에서 **로그인 사용자 토큰 + 역할**로 바뀌었다 (로컬 config.js의 DASH_KEY는 관리자로 계속 동작).
- **체크보드(상품 목록·유형·수동 칸·숨김)는 서버에 저장돼 모든 계정이 같은 보드를 본다** (2026-09-11, 워크스페이스 SSO로 들어온 마케터 포함). 처음 한 번은 대표님 브라우저의 보드가 그대로 공유본이 된다(예시 데이터는 안 올라감). 두 사람이 동시에 고치면 나중에 저장하는 쪽이 최신본을 다시 받고 "먼저 바꿨어요" 알림 — 방금 한 클릭만 다시 하면 된다. 마케터는 로그인하면 광고소재 대시보드 메뉴에서 시작한다.
- 셋업(1회): `./deploy-client-log.sh` 재실행 (shared_state 테이블 + client-log 함수).
- 등록 삭제는 '대기' 상태만 가능하고 Meta 보관함의 파일은 남는다.
